// EXAMPLE 1
const username = 'Kenn';

console.log(`${username} is the true JS Wizard!`);

// EXAMPLE 2
console.log(`Hey students, What bugs can ${username} fix for you today`);

// Example 3
const person = {
    username: 'Kenn',
    profession: 'Secret Agent'
}

console.log(`${person.username}, ${person.profession}`, 'Extraordinaire'); // Kenn, Secret Agent, Extraordinare